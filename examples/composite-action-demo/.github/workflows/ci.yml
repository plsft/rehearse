# AUTO-GENERATED by @gitgate/ci. Do not edit by hand.
# Run: gg ci compile
# Source: .gitgate\pipelines\ci.ts
# Docs: https://gitgate.com/docs/ci-quickstart

name: Composite action demo
on:
  pull_request: {}
  push:
    branches:
      - main
jobs:
  test:
    name: test
    runs-on: ubicloud-standard-4
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'
      - name: Set up deps via composite action
        uses: ./.github/actions/setup-deps
        with:
          node-version: 20.x
          cache-key-prefix: npm-demo
      - name: Run tests
        run: npm test
