# AUTO-GENERATED by @gitgate/ci. Do not edit by hand.
# Run: gg ci compile
# Source: .gitgate\pipelines\ci.ts
# Docs: https://gitgate.com/docs/ci-quickstart

name: CI
on:
  pull_request: {}
  push:
    branches:
      - main
      - master
permissions:
  contents: read
jobs:
  typecheck:
    name: typecheck
    runs-on: ubicloud-standard-4
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0
      - name: Setup pnpm
        uses: 'pnpm/action-setup@v4'
        with:
          version: 9.15.0
      - name: Setup Node 22
        uses: 'actions/setup-node@v4'
        with:
          node-version: '22'
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm turbo typecheck
  test:
    name: test
    runs-on: ubicloud-standard-4
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0
      - name: Setup pnpm
        uses: 'pnpm/action-setup@v4'
        with:
          version: 9.15.0
      - name: Setup Node 22
        uses: 'actions/setup-node@v4'
        with:
          node-version: '22'
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: 'Test @gitgate/ci'
        run: 'pnpm --filter @gitgate/ci test'
      - name: 'Test @gitgate/git-core'
        run: 'pnpm --filter @gitgate/git-core test'
  site:
    name: site
    runs-on: ubicloud-standard-2
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0
      - name: Setup pnpm
        uses: 'pnpm/action-setup@v4'
        with:
          version: 9.15.0
      - name: Setup Node 22
        uses: 'actions/setup-node@v4'
        with:
          node-version: '22'
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Build site
        run: 'pnpm --filter @gitgate/site build'
