# AUTO-GENERATED by @rehearse/ci. Do not edit by hand.
# Run: rh ci compile
# Source: .rehearse\pipelines\ci.ts
# Docs: https://rehearse.sh/docs/ci-quickstart

name: CI
on:
  pull_request: {}
  push:
    branches:
      - main
      - master
permissions:
  contents: read
jobs:
  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0
      - name: Setup pnpm
        uses: 'pnpm/action-setup@v4'
        with:
          version: 9.15.0
      - name: Setup Node 22
        uses: 'actions/setup-node@v4'
        with:
          node-version: '22'
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm turbo typecheck
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0
      - name: Setup pnpm
        uses: 'pnpm/action-setup@v4'
        with:
          version: 9.15.0
      - name: Setup Node 22
        uses: 'actions/setup-node@v4'
        with:
          node-version: '22'
          cache: pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Test ts-ci
        run: 'pnpm --filter @rehearse/ci test'
      - name: Test git-engine
        run: 'pnpm --filter @rehearse/git-core test'
